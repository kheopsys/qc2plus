models:
  - name: correlation_test_2
    description: "Comprehensive data quality plan for the level 2 models"

    qc2plus_tests:
      level2:
        correlation_analysis:
            variables: ["impressions", "clicks"]
            expected_correlation: 0.8
            threshold: 0.2
            correlation_type: "pearson"
            severity: critical
